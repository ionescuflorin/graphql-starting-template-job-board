type Query {
  job(id: ID!): Job
  jobs: [Job]
}

type Company {
  id: ID!
  name: String
  description: String
}

type Job {
  # ! means should never be null / reqired field
  id: ID!
  title: String
  company: Company
  description: String
}
